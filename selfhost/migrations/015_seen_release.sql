-- Какое объявление о новинках человек уже закрыл. Хранится в профиле, а не
-- в браузере: иначе окно всплывало бы заново на каждом устройстве и после
-- переустановки приложения.
alter table profiles
  add column if not exists seen_release varchar(32);
